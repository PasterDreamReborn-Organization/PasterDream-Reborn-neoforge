# 无名实体搬运任务书

> 目标：把原作「无名」（`shadow_npc_0`，NPC 对话实体）及其配套的**暗影窥视效果 / 暗影入侵事件 / 灯与影选择界面 / 剧情进度链**搬运到重写版。  
> 剧情文本以 `document/design/无名的对话.md` 为准，对话结构**完全按设计稿的两段对话**（不是原作 6 段右击）。

---

## 一、已确认决策

| 项 | 决策 |
|----|------|
| 对话文本/结构 | **完全按设计稿两段对话**（第一次对话 / 第二次对话 + 灯影分支），文本用设计稿措辞 |
| 进度 ID | 挂到 `story/` 路径下（符合重写版现有结构） |
| 灯与影选择界面 | **1:1 复刻**菜单 + Screen + 两个图片按钮 + 网络包 |
| 外部依赖进度 | `shadow_c_0`（d_0 的 parent）、`shadow_e_0`（暗影窥视解除门控）**暂不处理，留占位** |

---

## 二、原版组件清单（NOT_MODIFY 源文件）

### 1. 实体
| 原版文件 | 说明 |
|---------|------|
| `reference/net/pasterdream/entity/ShadowNpc0Entity.java` | GeckoLib `PathfinderMob`；免疫玩家攻击/火焰；`mobInteract` 右击触发对话 |
| `reference/net/pasterdream/entity/model/ShadowNpc0Model.java` | GeoModel → `geo/shadow_npc_0.geo.json` + `animations/shadow_npc_0.animation.json` |
| `reference/net/pasterdream/client/renderer/ShadowNpc0Renderer.java` | `GeoEntityRenderer`，`entityTranslucent` 半透明，scale 1 |
| `init/PasterdreamModEntities.java` | `SHADOW_NPC_0` 注册 |

资源：`geo/shadow_npc_0.geo.json`、`animations/shadow_npc_0.animation.json`、`textures/entities/shadow_npc_0.png`、`models/item/shadow_npc_0_spawn_egg.json`

### 2. 对话逻辑（文本改用设计稿）
| 原版文件 | 说明 |
|---------|------|
| `reference/net/pasterdream/procedures/ShadowNpc0Pr0Procedure.java` | 原版 6 段对话链；**仅参考其门控/授予/传送机制**，文本与段数按设计稿重写 |

### 3. 暗影窥视效果（未搬运）
| 原版文件 | 说明 |
|---------|------|
| `potion/ShadowSpyonBuffMobEffect.java` | `HARMFUL`；每 tick 调 `ShadowIntrudePr0Procedure`；`getCurativeItems()` 空（不可被牛奶解除） |
| `init/PasterdreamModMobEffects.java` | `SHADOW_SPYON`（`shadow_spyon` = 「暗影窥视」） |
| `procedures/LampShadowPr1Procedure.java` | 施加点：npc_2 完成 && shadow_e_0 未完成 → 32000 tick 暗影窥视 |

### 4. 暗影入侵事件
| 原版文件 | 说明 |
|---------|------|
| `procedures/ShadowIntrudePr0Procedure.java` | 主世界夜晚亮度 ≤7 概率触发；生成 `WeakenessTerrorbeak` + `ShadowHand`；结束授予 npc_3 并移除暗影窥视 |

### 5. 灯与影选择界面（未搬运，1:1 复刻）
| 原版文件 | 说明 |
|---------|------|
| `world/inventory/ShadowSelectEndMenu.java` | 菜单（无物品槽） |
| `client/gui/ShadowSelectEndScreen.java` | GUI 320×200，两个 `ImageButton`（黑暗/光明） |
| `network/ShadowSelectEndButtonMessage.java` | 网络包（buttonID 0=影 / 1=灯） |
| `procedures/ShadowSelectEndPr0Procedure.java` | 选**影** → 授予 d_0 + talent_shadow，给 `shadow_hilt` |
| `procedures/ShadowSelectEndPr1Procedure.java` | 选**灯** → 授予 d_0 + talent_light，给 `white_crystal` |
| `procedures/TrueShadowBedPr0Procedure.java` | 触发点：灯影之下、npc_5 完成 && d_0 未完成 → 右击暮影长床打开界面 |

界面资源：`textures/screens/shadow_select_end_gui.png`、`textures/screens/atlas/imagebutton_hei_an_an_niu.png`、`textures/screens/atlas/imagebutton_guang_ming_an_niu.png`

### 6. 相关物品
| 原版文件 | ID | 名称 | 重写版状态 |
|---------|-----|------|-----------|
| `item/ShadowHiltItem.java` | `shadow_hilt` | 凝影剑柄（stacksTo 64，tooltip「制作影刃的核心材料」） | ❌ 未搬运 |
| `item/TalentShadowItem.java` | `talent_shadow` | 占位物品（stacksTo 1） | ❌ 未搬运 |
| `item/TalentLightItem.java` | `talent_light` | 占位物品（stacksTo 1） | ❌ 未搬运 |
| — | `white_crystal` | 白水晶 | ✅ 已搬运 |

---

## 三、新进度方案（story/ 路径）

> 原版 6 段 npc 进度（`achievement_shadow_npc_0~5`）压成两段对话对应的逻辑标记，`shadow_d_0`/`talent_light`/`talent_shadow` 保留并改名。**方案可再微调。**

| 新 ID（`story/...`） | 含义 | 类型 | parent | 图标 | 对应原作 |
|----------------------|------|------|--------|------|---------|
| `story/shadow_npc_first_dialogue` | 第一次对话完成 | 隐藏标记 | — | shadow_npc_0 刷怪蛋 | npc_0~2 |
| `story/shadow_intrude_complete` | 暗影入侵事件完成（解锁第二次对话） | 隐藏标记 | — | — | npc_3 |
| `story/shadow_npc_second_dialogue` | 第二次对话完成 | 隐藏标记 | — | — | npc_4~5 |
| `story/shadow_choice` | 灯与影（做出选择） | 显示（task） | 占位 `shadow_c_0` | shadowcandle | d_0 |
| `story/talent_light` | 信仰光明 | 显示（task） | shadow_choice | talent_light | talent_light |
| `story/talent_shadow` | 暗影仆从 | 显示（task） | shadow_choice | talent_shadow | talent_shadow |

> 全部用 `minecraft:impossible` 触发器 + `PlayerTrigger`/程序 `award`；title/descr 需补全（原作多为空）。

---

## 四、搬运任务分解

### 阶段 A：实体本体
1. 新增 `ShadowNpc0Entity`（`world/entity/`，参考 `ShadowGhostEntity`）
2. 新增 `ShadowNpc0Model`（`client/model/`）+ `ShadowNpc0Renderer`（`client/renderer/`）
3. 搬运 geo / animation / 纹理 / 刷怪蛋模型资源
4. `ModEntities.java` 注册 `SHADOW_NPC_0` + `createAttributes`
5. `ModEntityRenderer.java` 注册渲染器 + 模型层
6. `ModCreativeModeTabs.java` 加刷怪蛋
7. datagen 补实体语言条目 + 刷怪蛋物品模型

### 阶段 B：暗影窥视效果 + 暗影入侵事件
8. 新增 `ShadowSpyonBuffMobEffect`（`world/effect/`）+ `ModEffects.java` 注册
9. 搬运暗影入侵逻辑（生成 WeaknessTerrorbeak + ShadowHand）
10. 施加点逻辑（第一次对话完成 → 给暗影窥视；`shadow_e_0` 门控留占位）

### 阶段 C：灯与影选择界面（1:1）
11. 新增 `ShadowSelectEndMenu` + `ModMenus.java` 注册
12. 新增 `ShadowSelectEndScreen` + `ModScreens.java` 注册（两按钮）
13. 新增网络包 + `ModNetwork.java` 注册（buttonID 0/1）
14. 搬运选影/选灯逻辑 + 暮影长床触发点（接入现有 `TwilightLantern`）
15. 搬运界面三张纹理

### 阶段 D：物品
16. 新增 `ShadowHiltItem`（`shadow_hilt`）
17. 新增 `talent_light` / `talent_shadow` 占位物品
18. datagen 补物品模型 + 语言

### 阶段 E：进度
19. `ModAdvancementProvider` 补 6 条进度（见第三节）
20. 补进度语言条目（title/descr）
21. 对话/事件/选择过程接入进度授予

### 阶段 F：对话串联
22. 按设计稿重写两段对话（`ShadowNpc0Pr0Procedure` 改写），接入传送/暗影窥视/门控
23. 把暗影入侵结束、灯影选择与进度门控串起来

---

## 五、现状盘点（重写版已复用）

已搬运：`WEAKENESS_TERRORBEAK`、`SHADOW_HAND`、`WHITE_CRYSTAL`、`TWILIGHT_LANTERN`（暮影长床）、`shadow_door` 音效、`LAMP_SHADOW_WORLD` 维度、SAN capability。

待搬运：实体 + 模型/渲染/资源、暗影窥视效果、暗影入侵逻辑、灯影选择界面、`shadow_hilt`/`talent_light`/`talent_shadow` 物品、6 条进度。
