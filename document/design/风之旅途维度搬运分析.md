# 风之旅途维度搬运分析

> PasterDream Reborn — `wind_journey_world` 维度搬运方案  
> 原作源码/资源位于 `NOT_MODIFY/`（严禁修改），本文档为搬运分析。  
> 范围：维度代码（群系 / 地物 / 维度类型与噪声 / 客户端特效 / 音效）。  
> **状态：✅ 维度代码与结构已全部实现（2026-08-15 更新）**。剩余为进游戏实测，见 §五。

---

## 〇、搬运状态总览（2026-08-15 更新）

| 模块 | 状态 |
|------|------|
| 音效 `breeze_wind` / `wind_journey`（注册 + ogg + sounds.json） | ✅ 已实现 |
| 群系标签 `wind_journey_world_biome` | ✅ 已实现（含两个群系） |
| 群系 ×2（`ModBiomes`） | ✅ 已实现 |
| 地物 ×8（`ModConfiguredFeatures` + `ModPlacedFeatures`） | ✅ 已实现（`ground_feature_6` 仍跳过） |
| 维度类型 / 噪声 / LevelStem（`ModDimensionTypes` / `ModNoiseSettings` / `ModLevelStems`） | ✅ 已实现 |
| 客户端 `WindJourneyDimension` + `LevelRendererMixin` / `FogRendererMixin` | ✅ 已实现 |
| lang（群系名、创造栏「风之旅途」） | ✅ 已实现 |
| 实体 ×7（萤火虫/雷云/高压雷云/骨翼/灰骨翼/水母/小石灵） | ✅ 已实现并接入群系生成（水母 creature、小石精 monster） |
| `small_stone_spirit_block` 方块 | ✅ 已实现并接入 `ground_feature_6` 地物 |
| `minecraft:bamboo_light`（原作 biome_0 自带） | ✅ 已接入 VEGETAL_DECORATION |
| 植物地物 ×4（茸毛苔/斩风草/风羽草/风岛芦苇） | ✅ 已实现并接入 VEGETAL_DECORATION |
| 鸟巢 `birds_nest` + 鹦鹉生成机制 | ✅ 已实现（BirdsNestBlock + GeckoLib 动画 + randomTick 生成鹦鹉） |
| 结构 ×34（NBT 转换 + 静态 JSON + structure_set，含补全圣诞树） | ✅ 已实现（2026-08-15，见 §2.3） |

---

## 一、维度概述

风之旅途是一个**浮空岛维度**，复用**原版末地浮空岛噪声**（`NoiseGeneratorSettings.end`）生成漂浮的云之岛群。默认方块 `thick_cloud`，默认流体 `water`，海平面 0，无含水层、无矿脉。

- 维度 ID：`wind_journey_world`
- 类型：`minecraft:noise` + `minecraft:multi_noise` 双群系
- 高度：噪声 0~128（末地岛屿范围），维度类型 `min_y=0 / height=256`（可建造高度，与末地一致，见 §三）

### 双群系划分（multi_noise）

| 群系 | 中文名 | 大陆性 continentalness | 温度 | 湿度 | 侵蚀 |
|------|--------|------|------|------|------|
| `wind_moor_archipelago` | 风泊群岛 | [-0.3, 1]（陆地） | [0,1] | [0,1] | [0,1] |
| `misty_dream_cloud_layer` | 迷梦云层 | [-1, -0.3]（云海） | [-1,0] | [-1,0] | [-1,0] |

地表规则（surface_rule，`ModNoiseSettings#makeWindJourneySurfaceRules`）：
- `wind_moor_archipelago`：`ON_FLOOR` + 无水 → `cyan_moss_stone`；`UNDER_FLOOR` → `cyan_stone`
- `misty_dream_cloud_layer`：整体 → `white_sand`

---

## 二、原作内容清单

### 2.1 群系

`wind_moor_archipelago`（风泊群岛）— 内容最丰富：
- 效果：sky/fog `-1376258`、water `-11475496`、water_fog `-9705764`、foliage `-11149912`、grass `-14107774`
- 音乐 `wind_journey`（min 12000 / max 24000，`replaceCurrentMusic=true`）；环境音 `breeze_wind`（tick_chance 0.0111）；粒子 `firefly_particle`（probability 0.003）
- 生成（均 MONSTER 类）：骨翼×6 / 灰骨翼×1 / 萤火虫×10 / 雷云×5 / 高压雷云×1
- 原作另有 creature 类：水母×10 / 小石灵×12 —— **当前代码注释仍标记「待搬运」，但实体已搬运，待回填**（见 §四/§五）

`misty_dream_cloud_layer`（迷梦云层）— 无生物生成（`MobSpawnSettings.EMPTY`），但实现中仍带环境音 + 音乐 + 1 个低位云团地物（原分析「空生成，仅地表」已不准确）。

颜色常量（biome_1）：foliage/grass 同 biome_0、sky/fog `-1114369`、water `-11475496`、water_fog `-5639444`。

### 2.2 地物（9 个，其中 8 个已实现）

| 原作 configured_feature | 实现 ID | 类型 | 目标群系 | step |
|------------------------|-------|------|---------|------|
| `congeal_wind_ore` | `CONGEAL_WIND_ORE` | ore(size11, 替换 thick_cloud) | biome_0 | underground_ores |
| `windrunner_crystal_ore` | `WIND_RUNNER_CRYSTAL_ORE` | ore(size6, 替换 cyan_stone) | biome_0 | underground_ores |
| `ground_feature_wind_journey_0` | `WIND_JOURNEY_CLOUD_PATCH` | 高位云团 random_patch | biome_0 | surface_structures |
| `ground_feature_wind_journey_1` | `WIND_JOURNEY_WATER_POOL` | lake(水 + cyan_stone 围边) | biome_0 | surface_structures |
| `ground_feature_wind_journey_2` | `WIND_JOURNEY_CLOUD_PATCH_LOW` | 低位云团 random_patch | biome_1 | surface_structures |
| `ground_feature_wind_journey_3` | `WIND_JOURNEY_PEBBLE_PATCH` | pebble | biome_0 | surface_structures |
| `ground_feature_wind_journey_4` | `WIND_JOURNEY_FIREFLY_NEST` | firefly_nest | biome_0 | vegetal_decoration |
| `ground_feature_wind_journey_5` | `WIND_JOURNEY_CYAN_MOSS_STONE_BLOB` | 泥→cyan_moss_stone blob | biome_0 | surface_structures |
| `ground_feature_wind_journey_6` | `WIND_JOURNEY_SMALL_STONE_SPIRIT` | small_stone_spirit_block | biome_0 | surface_structures |

> `ground_feature_wind_journey_6` 已落地：configured 为 `random_patch(tries=2/xz=5/y=2, simple_block small_stone_spirit_block)`，placed 为 `in_square + OCEAN_FLOOR + count=2`，与原作者 JSON 一致。

已实现 8 个地物的关键参数（`ModConfiguredFeatures` / `ModPlacedFeatures`）：

| 实现 ID | configured | placed |
|---------|-----------|--------|
| `CONGEAL_WIND_ORE` | ore size 11 | count 13, y 15~80 |
| `WIND_RUNNER_CRYSTAL_ORE` | ore size 6 | count 7, triangle y 40~80 |
| `WIND_JOURNEY_CLOUD_PATCH` | random_patch tries5/xz11/y4 | rarity 4, y 100~220 |
| `WIND_JOURNEY_CLOUD_PATCH_LOW` | random_patch tries4/xz11/y4 | rarity 4, y 40~65 |
| `WIND_JOURNEY_WATER_POOL` | lake(水+cyan_stone) | 每区块, WORLD_SURFACE_WG |
| `WIND_JOURNEY_PEBBLE_PATCH` | random_patch tries5/xz7/y3 | count 4, OCEAN_FLOOR |
| `WIND_JOURNEY_FIREFLY_NEST` | random_patch tries1/xz2/y1 | count 2, OCEAN_FLOOR |
| `WIND_JOURNEY_CYAN_MOSS_STONE_BLOB` | replace_blobs 泥→cyan_moss_stone r1~1 | count 7, rarity 5, 全高度 |

### 2.2b 植物地物（4 个，经 forge:add_features 注入）

| 原作 configured_feature | 实现 ID | 方块 | 原作参数 |
|------------------------|---------|------|---------|
| `grass_13` | `WIND_JOURNEY_HAIRY_MOSS_PATCH` | 茸毛苔 `hairy_moss` | random_patch tries48 / count3 / MOTION_BLOCKING |
| `grass_14` | `WIND_JOURNEY_CLEAVING_GRASS_PATCH` | 斩风草 `wind_cleaving_grass` | random_patch tries48 / count2 / MOTION_BLOCKING |
| `grass_15` | `WIND_JOURNEY_FEATHER_GRASS_PATCH` | 风羽草 `wind_feather_grass` | random_patch tries48 / count2 / MOTION_BLOCKING |
| `flower_18` | `WIND_JOURNEY_REED_PATCH` | 风岛芦苇 `wind_island_reed` | random_patch tries64 / count6 / rarity32 / WORLD_SURFACE_WG |

> 4 个植物均注入 biome_0 `vegetal_decoration`；内层为 `simple_block` + 纯空气过滤（`simpleBlockInAir`，不要求下方固体）。`wind_feather_grass` / `wind_island_reed` 为 `DoublePlantBlock`，由 `SimpleBlockFeature` 自动补齐上半段。

### 2.3 结构（34 个，已实现）

原作风之旅途维度实际有 **33 个结构**（非此前分析的 7 个），全部已搬运；另补全 1 个圣诞树结构（原作无自然生成配置，见文末）。采用**静态 structure/template_pool JSON**（复刻原作，仅改命名与群系）+ datagen 生成 structure_set 的方式：

- NBT：31 个用 `convert_structure` 转换，2 个复用染梦气泡 NBT。
- structure / template_pool：静态 JSON（`generateStructureFiles=false`），processor 均为 `block_ignore:minecraft:structure_block`。
- structure_set：由 `ModStructureSetProvider` 按 config 的 spacing/separation/salt 生成（独立 `random_spread`，weight=1）。
- 群系：静态 JSON 直接写单群系 ID（原作 `wind_journey_biome_0/1` → `wind_moor_archipelago` / `misty_dream_cloud_layer`）。

#### 风泊群岛（biome_0，8 个）

| 结构名（新） | 原 ID | step | 高度 | spacing/sep | salt |
|-------------|-------|------|------|-------------|------|
| `wind_island` | wind_island_0 | top_layer_modification | 62~75 | 7/4 | 610650667 |
| `windmill_lodge` | （同） | surface_structures | 100~120 | 73/36 | 270611341 |
| `break_wind_knight_altar` | （同） | surface_structures | 100~120 | 42/25 | 1344649511 |
| `windmoor_tree` | windmoor_tree_0 | top_layer_modification | 54~64 | 3/2 | 707951846 |
| `wind_infested_stone_0` | （同） | vegetal_decoration | 59~63 | 2/1 | 114139522 |
| `wind_infested_stone_1` | （同） | vegetal_decoration | 59~63 | 2/1 | 113947148 |
| `wind_pond` | wind_pond_0 | top_layer_modification | 59~63 | 3/2 | 737290576 |
| `picnic_basket` | picnic_basket_structure | surface_structures | absolute 1 | 20/10 | 1295591192 |

> `picnic_basket` 为跨维度结构：biomes 数组 `[minecraft:plains, dyedream_plains, dyedream_mushroom_mountains, dyedream_snowy_plains, wind_moor_archipelago]`。

#### 迷梦云层（biome_1，26 个）

| 结构名（新） | 数量 | 高度 | spacing/sep | 备注 |
|-------------|------|------|-------------|------|
| `hot_air_balloon_0~7` | 8 | 75~129（逐个递增） | 23~49 / 10~31 | 热气球 |
| `small_ballon_0~10` | 11 | 70~120 | 10/2 | 小气球 |
| `big_bubbles_6` / `big_bubbles_7` | 2 | 42~64 / 43~63 | 4/2、5/3 | **复用** `big_bubbles_2` / `big_bubbles_1` NBT |
| `bocchi_0` / `bocchi_1` | 2 | 78~140 | 32/19、120/34 | 彩蛋（孤独摇滚） |
| `breakwing_curtain` | 1 | 74~116 | 8/4 | 破翼帷幕 |
| `hakurei_reimu` | 1 | 78~140 | 33/20 | 彩蛋（东方灵梦） |
| `christmas_tree_island` | 1 | 55~80（贴地） | 48/24 | 圣诞树岛（**补全**，见下文） |

#### 命名处理

- 单变体删冗余后缀：`wind_island_0→wind_island`、`wind_pond_0→wind_pond`、`windmoor_tree_0→windmoor_tree`、`breakwing_curtain_0→breakwing_curtain`、`picnic_basket_structure→picnic_basket`。
- 修正原作拼写笔误（结构名与 NBT 名对齐）：`breakwind_curtain_0.nbt→breakwing_curtain.nbt`、`small_balloon_X.nbt→small_ballon_X.nbt`。

#### 圣诞树补全（非忠实搬运）

原作 `christmas_tree_0.nbt`（53×57×63）为「手动放置 `structure_block_23` 方块触发」的彩蛋结构，**无 worldgen 自然生成配置**（configured_feature / placed_feature / structure / biome features 均无引用）。本次作为迷梦云层稀有结构补全：去后缀命名 `christmas_tree_island`（圣诞树岛），贴地生成（`WORLD_SURFACE_WG`），spacing 48 / separation 24 / salt 885566331。结构含风旅方块（cloud/thick_cloud/windmoor_*/cyan_stone_brick_wall/christmas_lights）+ 原版圣诞树主体（云杉木/雪/羊毛）+ 多战利品箱。

### 2.4 实体与音效

- 实体：**7 个全部已搬运并接入群系生成** —— `firefly` / `thundercloud` / `highvoltage_thundercloud` / `bone_wing` / `ash_bone_wing` / `jellyfish` / `small_stone_spirit`（均见 `ModEntities`）。生成：monster 5 个 + 水母 creature×10 + 小石精 monster×12（原作 JSON 放 creature，但实体为 Monster 暗处生成，按 monster 接入）。
- 鹦鹉（vanilla `minecraft:parrot`）：原作**无群系自然生成**，仅通过未搬运的 `birds_nest`（鸟巢）方块 randomTick 生成（附近无鹦鹉时 10% 概率）。当前搁置，见 §五。
- 音效：`breeze_wind.ogg`、`wind_journey.ogg` 已拷贝至 `assets/pasterdream/sounds/`，`sounds.json` 已注册（breeze_wind 非流式、wind_journey 流式）。

---

## 三、维度参数要点

### 3.1 dimension_type（`WIND_JOURNEY_WORLD`）

`ModDimensionTypes` 已注册，与原作者 JSON 一致：`natural=false`、`piglin_safe=true`、`bed_works=true`、`respawn_anchor_works=false`、`coordinate_scale=1`、`ambient_light=0`、`min_y=0`、`height=256`、`logical_height=256`、`effects=pasterdream:wind_journey_world`、monster 生成光照 0~7。

### 3.2 噪声设置（`WIND_JOURNEY_WORLD`）

复用**原版末地**噪声路由（`NoiseGeneratorSettings.end(context)`），仅替换默认方块/流体/地表规则：

- `noiseSettings`：末地（`min_y=0, height=128, island_noise_override`）
- `default_block`：`thick_cloud`；`default_fluid`：`water`
- `noiseRouter`：末地浮空岛噪声路由（`end_islands` + `sloped_cheese`）
- `surface_rule`：`makeWindJourneySurfaceRules`（§一）
- `spawn_target`：空 `List.of()`
- `sea_level`：0；`disable_mob_generation`：false；`aquifers_enabled`：false；`ore_veins_enabled`：false；`use_legacy_random_source`：true

> 高度说明：维度类型 `height=256` 而噪声 `height=128` 与**原版末地**一致（256 为可建造高度，128 为岛屿生成范围，128~256 留空供高位云团/结构）。实现按末地原样保留，无需修改。

### 3.3 LevelStem（`WIND_JOURNEY_WORLD`）

`ModLevelStems` 用 `MultiNoiseBiomeSource` 双群系 + `NoiseBasedChunkGenerator`，参数与 §一 表格一致（六维：温度/湿度/大陆性/侵蚀/怪异度/偏移，怪异度取 point(0)，偏移 span[-1,1]）。

### 3.4 客户端特效（`effects=pasterdream:wind_journey_world`）

实际实现与原分析有差异，以代码为准：

- `WindJourneyDimension`（`world/dimension/WindJourneyDimension.java`）注册 `DimensionSpecialEffects`：`cloudLevel=NaN`（无云）、`hasGround=true`、`SkyType.NONE`（**无太阳/月亮**，非原分析的 SkyType.NORMAL）、`forceBrightLightmap=false`、`constantAmbientLight=false`；`getBrightnessDependentFogColor` 返回固定雾色 `(0.533, 0.957, 0.922)`（青白），`isFoggyAt` 恒 false。
- 两个 mixin 配合：
  - `LevelRendererMixin`：在 `wind_journey_world` 取消原版云渲染（`pasterdream$cancelWindJourneyClouds`）。
  - `FogRendererMixin`：在 `wind_journey_world` 应用自定义雾色逻辑。

> 原分析设想「SkyType.NORMAL + sky/fog `#EB1222`」未采用，改为「无天空 + 固定青白雾 + 禁用原版云」。群系内 sky/fog 色仍按原作常量写进 `ModBiomes`（`-1376258` / `-1114369`）。

---

## 四、当前项目现状

### 已搬运（维度代码全部落地）
- 群系 ×2：`WIND_MOOR_ARCHIPELAGO` / `MISTY_DREAM_CLOUD_LAYER`（`ModBiomes`）
- 地物 ×8：configured + placed（`ModConfiguredFeatures` / `ModPlacedFeatures`，见 §2.2 参数表）
- 群系标签：`wind_journey_world_biome` = { 两个群系 }（`ModBiomeTagsProvider`，注意已含 biome_1，非仅 biome_0）
- 维度三件套：`ModDimensionTypes.WIND_JOURNEY_WORLD` / `ModNoiseSettings.WIND_JOURNEY_WORLD` / `ModLevelStems.WIND_JOURNEY_WORLD`
- 客户端：`WindJourneyDimension` + `LevelRendererMixin` + `FogRendererMixin`
- 音效：`ModSounds.BREEZE_WIND` / `ModSounds.WIND_JOURNEY` + ogg + sounds.json
- lang：群系名（风泊群岛 / 迷梦云层，zh + en）、创造栏「风之旅途」（itemGroup）
- 实体 ×7：含骨翼/灰骨翼/水母/小石灵；方块 `SMALL_STONE_SPIRIT_BLOCK`
- 维度引用键：`WindJourneyDimension.WIND_JOURNEY_WORLD`（ResourceKey\<Level\>）
- 结构 ×34：NBT 转换 + 静态 structure/template_pool JSON + datagen structure_set（`ModStructureConfig`，见 §2.3）
- lang：34 个结构中英文翻译键（`structure.pasterdream.*`）

> 备注：`JellyfishEntity.java` 有未提交的微调（诱惑速度 1.0→1.25、移动/飞行速度 0.2→0.35），属实体调参，非维度代码。

---

## 五、后续（另行排期）

1. **实测**：进游戏验证浮空岛生成、群系分布、云团/水池/萤火虫巢/小石堆/植物等地物、水母/小石精生成、客户端雾与无云效果，以及 33 个结构的生成（风泊群岛结构、迷梦云层热气球/小气球/气泡等）。

---

## 六、风险与待确认

1. ~~**高度不一致**~~：已确认与末地一致（256 建造高度 + 128 噪声），实现按原样保留。
2. **客户端特效偏离原作**：采用「SkyType.NONE + 固定青白雾 + 禁用原版云」而非原分析「SkyType.NORMAL + `#EB1222`」。若需还原原作天空表现，需改 `WindJourneyDimension` 与两个 mixin。
3. **`bamboo_light`**：biome_0 原自带 `minecraft:bamboo_light`，实现未加入，是否保留待定。
4. **小石精类别偏差**：原作 JSON 将小石精放在 `creature`（weight 12），但实体 `SmallStoneSpiritEntity extends Monster` 且生成判定用 `Monster.isDarkEnoughToSpawn`（暗处生成），故实现按 `monster` 接入，否则永不生成。若需严格还原原作 category，需同步改造实体。
