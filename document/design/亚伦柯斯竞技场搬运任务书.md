# 亚伦柯斯竞技场搬运任务书

> 目标：把「亚伦柯斯之触」BOSS 战的载体——**亚伦柯斯竞技场系统**搬运到重写版。BOSS 双手实体、技能、Boss 条与全部支撑实体/方块（魔法球/符文塔/暗影之手/恐惧喙/漩涡方块）均已就位，本任务书只覆盖**剩余的竞技场系统本体**。
>
> 核心方块两个：`aaroncos_arena_portals`（亚伦柯斯竞技场传送门）、`aaroncos_eye`（亚伦柯斯之眼，原名 `aaroncoshandspawnblock`）。

---

## 一、已确认决策

| 项 | 决策 |
|----|------|
| 生成方块重命名 | `aaroncoshandspawnblock` → **`aaroncos_eye`**（亚伦柯斯之眼），语义化 snake_case |
| 唱片重命名 | `aaroncos_disc` → **`aaroncos_music_disc`**（遵循 `_disc` → `_music_disc` 规范，参考 `sweetdream_disc`→`sweet_dream_music_disc`） |
| Procedure 处理 | 与暗影地牢/暮影之笼一致——逻辑**直接集成到 Block/TileEntity/Handler**，不保留独立 Procedure 类 |
| 进度门控 | `achievement_shadow_d_0` / `achievement_shadow_e_0` / `achievement_talent_light` / `achievement_talent_shadow` **留占位**，等成就体系就绪后接入 |
| 结构放置 | 竞技场结构 `aaroncos_arena.nbt` 在**玩家进入维度时手动 `placeInWorld`**（不走 datagen structure provider） |
| 参考模板 | `world/block/windknightaltar/`（GeckoLib 动画方块实体 + Procedure）、`world/block/shadowvortex/`（方块实体全家桶）、`world/block/twilightlantern/`（tick 波次） |

---

## 二、原版组件清单（NOT_MODIFY 源文件）

### 2.1 方块（3 个）

| 原版类 | 原版 ID | 新 ID | 说明 |
|--------|---------|-------|------|
| `AaroncoshandspawnblockBlock` | `aaroncoshandspawnblock` | `aaroncos_eye` | 亚伦柯斯之眼，生成控制器。`BaseEntityBlock`，GeckoLib 动画，`strength(-1,3600000)` 不可破坏、发光 12、`noOcclusion`、emissive、`RenderShape.ENTITYBLOCK_ANIMATED`、`ANIMATION 0/1`、`getShape box(1,-2,7,15,18,9)`。`onPlace` scheduleTick 20 → `tick` 执行 Pr1 → 再 scheduleTick 20；`use` 执行 Pr0 |
| `AaroncosHandChestBlock` | `aaroncos_hand_chest` | `aaroncos_hand_chest` | 奖励箱。`BaseEntityBlock`，GeckoLib 动画，`strength(-1,3600000)`、`SoundType.STONE`、`ANIMATION 0/2`、`getShape box(-2,0,6,18,16,14)`。`use` 执行 Pr0（开箱）；`onRemove` dropContents；红石比较器输出 |
| `AaroncosArenaPortalsBlock` | `aaroncos_arena_portals` | `aaroncos_arena_portals` | 传送门。`SlabBlock`，`strength(-1,3600000)`、发光 15、`noCollision`、`noOcclusion`、emissive、`dynamicShape`、`Basedrum`。`entityInside` 执行 Pr0（传送） |

### 2.2 方块实体（2 个）

| 原版类 | 说明 |
|--------|------|
| `AaroncoshandspawnblockTileEntity` | 生成控制器 BE，`persistentData` 存 `switch`(boolean)、`time0`(double) |
| `AaroncosHandChestTileEntity` | 奖励箱容器 BE |

### 2.3 模型 / 渲染器 / 展示物品（随方块捆绑搬运）

| 原版类 | 用途 |
|--------|------|
| `AaroncosHandChestBlockModel` / `AaroncosHandChestDisplayModel` | 奖励箱方块/物品展示模型 |
| `AaroncoshandspawnblockBlockModel` / `AaroncoshandspawnblockDisplayModel` | 生成控制器方块/物品展示模型 |
| `AaroncosHandChestTileRenderer` / `AaroncosHandChestDisplayItemRenderer` | 奖励箱方块实体/物品渲染器 |
| `AaroncoshandspawnblockTileRenderer` / `AaroncoshandspawnblockDisplayItemRenderer` | 生成控制器方块实体/物品渲染器 |
| `AaroncosHandChestDisplayItem` / `AaroncoshandspawnblockDisplayItem` | 对应 BlockItem 展示项 |

### 2.4 物品（2 个 + 3 个掉落物）

| 原版类 | 原版 ID | 新 ID | 说明 |
|--------|---------|-------|------|
| `AaroncosArenaCreateItem` | `aaroncos_arena_create` | `aaroncos_arena_create` | 创建竞技场物品，`stacksTo(1)`，tooltip `§4创建模式物品`，`use` 执行传送门逻辑 |
| `AaroncosDiscItem` | `aaroncos_disc` | `aaroncos_music_disc` | 音乐唱片，`RecordItem`，`aaroncos_music`，2980 tick，`Rarity.RARE` |

### 2.5 维度（1 个）

| 原版类 | 说明 |
|--------|------|
| `AaroncosArenaWorldDimension` | `DimensionSpecialEffects`（雾色 0.2/0.2/0.2、`isFoggyAt`=true）+ `PlayerChangedDimensionEvent` → Pr0 |

### 2.6 Procedure（5 个）

| 原版 Procedure | 触发方 | 核心逻辑 |
|---------------|--------|----------|
| `AaroncoshandspawnblockPr0Procedure` | 生成控制器 `use` | 维度检查 + `switch` 门控 → `switch=true` → 生成左右手（x±12）→ 播放 `aaroncos_music` → 玩家改 ADVENTURE |
| `AaroncoshandspawnblockPr1Procedure` | 生成控制器 `tick` | `switch=true` 时 `time0` 递增；`time0=50/100/150` 各生成 2 只 Terrorbeak（z±12）；双手死亡后 → 放奖励箱（y-1）+ 销毁自身 + 授予 `achievement_shadow_e_0` + 移除 SHADOW_SPYON + 倒计时提示 → 传送回主世界重生点 + 清理非玩家实体 |
| `AaroncosHandChestPr0Procedure` | 奖励箱 `use` | `switch` 门控 → `switch=true` + animation=1 + 音效 + 粒子 → 按成就掉落（talent_light→white_flower_body/white_crystal，talent_shadow→degenerate_bodys/shadow_hilt）+ 固定掉落 pure_horror → 41t 后销毁 |
| `AaroncosArenaPortalsPr0Procedure` | 传送门 `entityInside` / 创建物品 `use` | 检查 `achievement_shadow_d_0` 或创造 → 传送 `aaroncos_arena_world` (0,70,0) + SLOW_FALLING + ADVENTURE；否则提示「尚未完成前置进度」 |
| `AaroncosArenaWorldPr0Procedure` | 进入维度 | `players.size()<2` 时 `placeInWorld` `aaroncos_arena` 结构（-35,0,-35）+ 清理实体 + 传送 (0,70,0) |

### 2.7 资源文件

| 类别 | 文件 |
|------|------|
| Geo 模型 | `geo/aaroncos_hand_chest.geo.json`、`geo/aaroncos_hand_spawn_block.geo.json` |
| 动画 | `animations/aaroncos_hand_chest.animation.json`、`animations/aaroncos_hand_spawn_block.animation.json` |
| 纹理 | `textures/block/aaroncos_hand_chest.png`、`textures/block/aaroncos_hand_spawn_block.png`（→ 改名 `aaroncos_eye.png`） |
| 音效 | `aaroncos_music.ogg`（2.4MB，**已搬运**）、`aaroncos_spawn.ogg`（**已搬运**） |
| 结构 NBT | `structures/aaroncos_arena.nbt`（1.0MB，竞技场主体，本任务书搬运）；`aaroncos_arena_portals.nbt`（11.6KB，灯影之下维度世界生成结构，不属本任务书） |

---

## 三、依赖项盘点

### 3.1 已搬运（直接复用，无需再动）

| 类别 | 项                                                                                                                           |
|------|-----------------------------------------------------------------------------------------------------------------------------|
| 实体 | `aaroncos_left_hand` / `aaroncos_right_hand`、`terrorbeak`（3 变体）、`shadow_hand`、`shadow_magicball`、`shadow_tune_totem`        |
| 方块 | `shadow_vortex`                                                                                                             |
| 效果 | `shadow_spyon`（暗影窥视）、`confusion`、`shadow_silence`、`restrainmove_block`                                  |
| 音效 | `aaroncos_music`、`aaroncos_spawn`、`shadow_door`                                                                             |
| 粒子 | `shadow_stone_particle`、`dust_0_particle`                                                                                   |
| 物品 | `white_crystal`、`shadow_hilt`、双手刷怪蛋                                                                                         |
| 调度 | `PasterDreamMod.queueServerWork(int, Runnable)`（签名与原名一致）                                                                    |
| 掉落物品 | `brooch_of_white_orchid`（原名 white_flower_body，白兰花胸针）、`seal_of_the_corrupted`（原名 degenerate_bodys，堕落者之印）、`pure_horror`（纯粹恐惧） |


### 3.2 未搬运（本任务书需补）

| 类别 | 项 | 说明 |
|------|-----|------|
| 维度数据 | `aaroncos_arena_world`（DimensionType + NoiseSettings + LevelStem）、`aaroncos_arena` | datagen 产出 |
| 结构 NBT | `aaroncos_arena.nbt`（竞技场主体） | 需做方块 ID 替换（`convert_structure.exe`） |

> 传送门结构 `aaroncos_arena_portals.nbt` 是**灯影之下（lamp_shadow_world）维度的世界生成结构**（竞技场入口），不属本任务书搬运范围，随灯影之下维度世界生成一并处理；`aaroncos_arena.nbt` 才是竞技场主体，由 `AaroncosArenaWorldPr0Procedure` 在进入维度时手动放置。

---

## 四、搬运任务分解

### 阶段 A：维度 + 结构（地基）
1. `worldgen/dimension/ModDimensionTypes.java` 注册 `AARONCOS_ARENA_WORLD` 维度类型
2. `worldgen/dimension/ModNoiseSettings.java` 注册竞技场噪声设置（虚空/小空间，可参考 dyedream_world）
3. `worldgen/dimension/ModLevelStems.java` 注册维度 LevelStem（关联 `aaroncos_arena`）
4. `worldgen/biome/ModBiomes.java` 注册 `aaroncos_arena`
5. 新增 `world/dimension/AaroncosArenaWorldDimension.java`（`DimensionSpecialEffects` 雾效 + `PlayerChangedDimensionEvent`，参考 `LampShadowWorldDimension`）
6. 搬运并转换 `aaroncos_arena.nbt`（ID 替换后放入 `data/pasterdream/structures/`）

### 阶段 B：生成控制器方块（亚伦柯斯之眼）
7. 新增 `world/block/aaroncoseye/AaroncosEyeBlock.java`（原名 spawnblock，GeckoLib 动画，参考 `shadowvortex`/`windknightaltar`）
8. 新增 `world/block/aaroncoseye/AaroncosEyeTileEntity.java`（persistentData 存 `switch`/`time0`）
9. 新增模型 `AaroncosEyeBlockModel` / `AaroncosEyeDisplayModel` + 渲染器 `AaroncosEyeTileRenderer` / `AaroncosEyeDisplayItemRenderer` + 展示项 `AaroncosEyeDisplayItem`
10. `ModBlocks` / `ModBlockEntities` / `ModBlockEntityRenderer` / `ModItems` 注册
11. 搬运 geo / animation / 纹理资源（纹理改名 `aaroncos_eye.png`）

### 阶段 C：奖励箱方块
12. 新增 `world/block/aaroncoshandchest/AaroncosHandChestBlock.java`（GeckoLib 动画 + 容器 BE）
13. 新增 `world/block/aaroncoshandchest/AaroncosHandChestTileEntity.java`（容器）
14. 模型 / 渲染器 / 展示项 + 注册（同上）
15. 搬运 geo / animation / 纹理资源

### 阶段 D：传送门方块 + 物品
16. 新增 `world/block/AaroncosArenaPortalsBlock.java`（`SlabBlock`，`entityInside` 触发传送，无 BE）
17. 新增 `world/item/AaroncosArenaCreateItem.java`（创建竞技场物品，`use` 复用传送门逻辑）
18. 新增 `world/item/AaroncosMusicDiscItem.java`（唱片，`RecordItem` + `aaroncos_music`）
19. 注册 + datagen 补 blockstate/模型/语言

### 阶段 E：战斗/交互逻辑（5 个 Procedure 集成）
20. `AaroncosEyeBlock.use` 集成 Pr0 逻辑（激活生成双手）
21. `AaroncosEyeTileEntity.serverTick`（或独立 `AaroncosArenaBattleHandler`）集成 Pr1 战斗时间轴
22. `AaroncosHandChestBlock.use` 集成开箱掉落逻辑
23. `AaroncosArenaPortalsBlock.entityInside` + 创建物品 `use` 集成传送逻辑
24. `AaroncosArenaWorldDimension` 集成进入维度放置结构逻辑

### 阶段 F：掉落物 + 进度占位 + 语言
25. 搬运/占位 1 个掉落物品（`pure_horror`）
26. 进度门控用占位条件（成就未搬运前用临时判断或固定放行）
27. datagen 补全部语言条目 + 创造物品栏分类

---

## 五、关键实现细节

### 5.1 生成控制器（亚伦柯斯之眼）战斗时间轴

Pr1 逻辑按 `time0`（每 20tick 自增 1，因为方块 `tick` 以 20tick 调度）推进：

| time0 | 事件 |
|-------|------|
| 50 / 100 / 150 | 各生成 2 只 Terrorbeak（`z±12`） |
| ≥150 后 | `time0` 复位为 0，循环播放 `aaroncos_music` |
| 双手死亡（99 格范围内空） | 放奖励箱（`y-1`）+ 销毁自身 + 授予进度 + 移除暗影窥视 + 倒计时 20s 传送回主世界 + 清理非玩家实体 |

> 原作 `getShape box(1,-2,7,15,18,9)` 碰撞箱很薄（2 格厚竖板），是「眼」的立牌造型，搬运时保留。

### 5.2 奖励箱掉落逻辑

| 条件 | 掉落 |
|------|------|
| 玩家完成 `talent_light` | `brooch_of_white_orchid` + `white_crystal` |
| 玩家完成 `talent_shadow` | `seal_of_the_corrupted` + `shadow_hilt` |
| 固定 | `pure_horror` |

> 开箱后 `animation` 0→1、播放 `shadow_door`、粒子（shadow_stone/end_rod/smoke/dust_0），41t 后销毁自身。奖励箱为一次性。

### 5.3 传送与维度

- 传送门（`entityInside`）与创建物品（`use`）共用同一传送逻辑：检查 `achievement_shadow_d_0` 或创造 → 传送到 `aaroncos_arena_world` 坐标 (0,70,0)，给 SLOW_FALLING 120t，改 ADVENTURE；失败提示「尚未完成前置进度」。
- 进入维度时（`PlayerChangedDimensionEvent` → Pr0）：`players.size()<2` 才放置竞技场结构（防重复），清理 (0,50,0) 半径 99 的非玩家实体，传送 (0,70,0)。
- 战斗结束：倒计时 20s 后传送回**主世界重生点**（玩家重生点，否则世界出生点），改 SURVIVAL，清理竞技场内非玩家实体。

### 5.4 进度占位

| 原版进度 | 触发点 | 占位方案 |
|---------|--------|----------|
| `achievement_shadow_d_0` | 传送门进入前提 | 临时移除检查或用 `true` 放行 |
| `achievement_shadow_e_0` | 战斗完成授予 | 跳过授予，仅走剩余流程 |
| `achievement_talent_light` / `achievement_talent_shadow` | 奖励箱掉落分支 | 临时用占位标记判断（或固定只走固定掉落 `pure_horror`） |

---

## 六、ID 映射表待登记项

> 以下条目需在 `document/design/ID映射表.md` 中登记（方块映射表）。

### 方块

| 旧 ID | 新 ID | 变更说明 | 客制化类处理 |
|-------|-------|---------|------------|
| `aaroncoshandspawnblock` | `aaroncos_eye` | 重命名，语义化（亚伦柯斯之眼） | 客制化类 GeckoLib 动画方块实体，竞技场生成控制器 |
| `aaroncos_hand_chest` | `aaroncos_hand_chest` | 不改 | 客制化类 GeckoLib 动画方块实体 + 容器，BOSS 奖励箱 |
| `aaroncos_arena_portals` | `aaroncos_arena_portals` | 不改 | 客制化类 `AaroncosArenaPortalsBlock`（SlabBlock），竞技场传送门 |

### 物品

| 旧 ID | 新 ID | 变更说明 | 客制化类处理 |
|-------|-------|---------|------------|
| `aaroncos_arena_create` | `aaroncos_arena_create` | 不改 | `AaroncosArenaCreateItem`，创建竞技场（调试传送） |
| `aaroncos_disc` | `aaroncos_music_disc` | 重命名 `_disc`→`_music_disc` | 无需客制化类，直接用 `RecordItem`（播放 `aaroncos_music`，2980 tick），BOSS 音乐唱片 |
| `white_flower_body` | `brooch_of_white_orchid` | 重命名（白兰花胸针） | 客制化类 `BroochOfWhiteOrchidItem`，实现 `ICurioItem`，免疫环境降SAN效果，品质 EPIC |
| `degenerate_bodys` | `seal_of_the_corrupted` | 重命名（腐化之印） | 客制化类 `SealOfTheCorruptedItem`，实现 `ICurioItem`，生命-4 / 触及+0.2 / 方块触及+1.0 / 攻击+2.0，品质 EPIC，body 槽位 |
| `pure_horror` | `pure_horror` | 不改 | 待搬运（奖励箱掉落） |

---

## 七、建议搬运顺序

```
第 1 步：维度 datagen（DimensionType + NoiseSettings + LevelStem + biome）+ AaroncosArenaWorldDimension
第 2 步：转换 aaroncos_arena.nbt（convert_structure.exe 替换方块 ID）
第 3 步：注册 aaroncos_eye 方块 + BlockEntity + 模型/渲染器（纯框架，不含战斗逻辑）
第 4 步：注册 aaroncos_hand_chest 方块 + 容器 BlockEntity + 模型/渲染器（纯框架）
第 5 步：注册 aaroncos_arena_portals 传送门方块 + aaroncos_arena_create 物品 + 唱片物品
第 6 步：集成 5 个 Procedure 逻辑（激活 / 战斗时间轴 / 开箱 / 传送 / 进入维度）
第 7 步：搬运 3 个掉落物品 + 进度占位
第 8 步：datagen 补语言 / 模型 / 创造物品栏
第 9 步：联调（进入竞技场 → 激活 BOSS → 战斗 → 奖励 → 传送回主世界）
```

> 传送门结构 `aaroncos_arena_portals.nbt` 属灯影之下维度的世界生成，不属本任务书搬运范围（第 2 步仅转换 `aaroncos_arena.nbt`）。
